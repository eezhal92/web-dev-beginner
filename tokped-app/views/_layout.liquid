<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>tokped</title>
  <link rel="stylesheet" href="/static/css/main.css">
</head>
<body>

  <header class="header">
    <div class="header-left">
      <div>
        <a href="/" class="brand-link">tokped</a>
      </div>
      <nav class="main-nav">
        <ul>
          <li><a href="/">Jelajahi Toko</a></li>
          <li><a href="/product-catalog">Katalog</a></li>
        </ul>
        <form method="GET" action="/product-catalog">
          <input name="keyword" class="search-box" role="search" placeholder="Cari produk" />
          <button class="btn btn--gray">🔍</button>
        </form>
      </nav>
    </div>

    <div><button class="btn btn--primary add-toko">Tambah Toko</button></div>
  </header>


  <div class="modal">
    <div class="modal-dialog">
      <div class="modal-header">Tambah Toko
        <button  class="close-button">&times;</button>
      </div>
      <form tabindex="0" class="form" method="POST" action="/create-shop" enctype="application/x-www-form-urlencoded">
        <div class="modal-body">

          <div class="form-group">
            <label for="">Name</label>
            <input name="name" required>
          </div>

          <div class="form-group">
            <label for="">Address</label>
            <select name="address">
              {% for city in addresses %}
              <option value="{{ city }}">{{ city }}</option>
              {% endfor %}
            </select>
          </div>

          <div class="form-group">
            <label for="">No HP</label>
            <input name="phone" maxlength="12">
          </div>

        </div>

        <div class="modal-footer">
          <button class="btn btn--primary">Submit</button>
          <button type="reset" class="btn-cancel btn btn--default">Cancel</button>
        </div>
      </form>

    </div>
  </div>

  {% block %}{% endblock %}
</body>
</html>
